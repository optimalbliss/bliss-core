
// actually render using Riot.js

Renderer = module.exports = Class.extend({
	
   initialize: function(options) {
		
		Object.assign(this, options);
   },
   
   render: function() {
		
		return false;
	}
});
